# ai_vdb_provider_faiss
Drupal module providing a FAISS vector database provider for the AI VDB ecosystem. Forked and adapted from the OpenSearch provider, this module enables integration of FAISS as a backend for vector search in Drupal via Search API.
